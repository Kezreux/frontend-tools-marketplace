---
description: Run a WCAG 2.1 AA accessibility audit on the current git diff or a specified path. Read-only.
argument-hint: [path-or-glob]
---

Run the `a11y-audit` skill.

- If `$ARGUMENTS` is non-empty, audit those paths.
- Otherwise, default to the current git diff.

Return the skill's documented report format verbatim — no preamble.
